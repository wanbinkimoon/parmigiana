// import { fromJS } from 'immutable';
// import { makeSelectProjectionDomain } from '../selectors';

// const selector = makeSelectProjectionDomain();

describe('makeSelectProjectionDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
