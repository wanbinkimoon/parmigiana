import styled, { css } from 'styled-components';

export const SvgWrap = styled.div`
  display: flex;
  justify-content: center;
  align-items: center;
  width: 100%;

  & > svg {
    width: 100%;
    height: 100%;
  }

  #Wrap {
    fill: none;
    stroke: orange;
    stroke-width: 1;
  }

  #Logo {
    fill: orange:
  }
`